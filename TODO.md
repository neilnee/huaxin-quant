# TODO

---

## 模型四：Tracker 完善

Bloom 信号层已完成（发现侧），下阶段补齐持仓管理和估值触发，形成完整的"发现 → 跟踪 → 估值 → 交易"闭环。

### P0 — 持仓管理模块

持仓管理同时覆盖个股和 ETF，两者策略独立、同在一个模块内统一调度。

**个股持仓：**
- [ ] 消费 Bloom 信号 + 模型三估值结果 + 实际持仓成本
- [ ] 止损/止盈/加仓/减仓 建议，基于估值安全边际 + 量价结构变化
- [ ] 持仓股票无论模型二是否覆盖，都独立管理；优先保护已有仓位风险
- [ ] 分批建仓跟踪：批次成本、止损线上移、仓位占比

**ETF 持仓：**
- [ ] ETF 独立策略，不依赖 Bloom 信号（ETF 无 VCP 形态概念）
- [ ] 消费：板块热度 + 指数估值分位 + 折溢价 + 持仓成本
- [ ] 定投/网格/趋势 等多策略支持，初始先落地一种
- [ ] ETF 与个股共享统一的持仓状态表结构

**产出：**
- [ ] **instructions/signal-position.md**：持仓管理指令卡（个股 + ETF 双轨）
- [ ] **scripts/position.py**：持仓管理脚本，统一调度个股和 ETF
- [ ] **strategies/04-position.json**：持仓管理策略配置

### P0 — 策略验证

模型四是每日持续跟踪，从跟踪结果反向验证策略有效性。

- [ ] **信号胜率统计**：Bloom 信号出现后 N 日走势 vs 基准
  - TRIGGERED / MATURE / FORMING 各自统计
  - 不管是否实际买入，纯策略角度回看
- [ ] **持仓表现跟踪**：实际持仓的盈亏、最大回撤、持仓天数
- [ ] **定期复盘报告**：周/月维度汇总策略表现，指导参数调整

### P0 — 估值触发层

- [ ] **instructions/signal-valuation-queue.md**：估值触发指令卡
  - 消费 Bloom 输出（valuation_candidate + valuation_priority）
  - 判断哪些股票值得进入模型三估值流程
  - 已有估值是否需要复核（过期、价格偏离过大等）
- [ ] 估值触发逻辑集成到 Tracker 主流程

### P1 — Tracker 总控

- [ ] **scripts/tracker.py 重构**：统一调度 Bloom → 估值触发 → 持仓管理
- [ ] 总控指令卡 `instructions/04-tracker.md` 对齐三模块完整流程

---

## 独立市场状态与板块热度模块

处于模型二量化分析环节的旁路环境层：独立运行，不读取或改写 Pool、Quant、Bloom 输出。第一版使用通达信宽基/个股行情和通达信板块快照，不使用外部资金流。

- [x] **模块设计与数据源能力验证**：六个宽基 300 日线、通达信行业映射（`tdxhy.cfg + incon.dat`）及三类板块文件、分块下载修复均已验证。
- [x] **共享市场数据层**：`scripts/data/` 下统一管理通达信下载、SQLite、板块日快照、初始化/增量更新与覆盖率校验；市场模块为首个消费者。
- [x] **市场与板块首版计算**：趋势、波动、全 A 广度，以及行业/概念/风格/指数四类热度、轮动、全市场/行业/概念 RPS 与 Markdown 日报输出，且不依赖模型二结果。
- [x] **HTML 市场日报首版**：脚本输出页面专用 JSON；静态页面展示市场状态、宽基趋势、板块排名与展开详情、20日排名矩阵及广度。
- [x] **市场五状态机**：统一合成广度口径，新增弱势收敛过渡状态，并以连续主线或高轮动证据细分选择性结构展示。
- [x] **统一数据分析面板壳**：本地 `dashboard/` 作为全系统展示入口；市场模块发布独立数据包，Pool/VCP/Bloom/持仓/估值后续以适配层接入，不改动既有输出。
- [x] **市场指标与板块状态说明**：市场环境页底部固定展示阶段位置、近期量能、短期趋势、波动分档及板块阶段状态的实际计算口径和含义。
- [x] **板块状态兜底修正**：新增“强势初现”，将“观察中”作为中性兜底；“弱势退潮”必须具有此前强势、随后恶化的生命周期证据。
- [x] **板块阶段基线回填**：运行时自动以当前成分快照补齐最近20个交易日的缺失板块指标，并明确区分非点时回填与每日快照口径。
- [x] **今日盘面主线**：独立计算当日板块强度和核心股，结合按日缓存的妙想资讯，由受约束 LLM 归纳唯一主线、核心事件与叙事逻辑；不改写市场状态和 5/20 日板块生命周期。
- [x] **今日主线催化时序门禁**：资讯拆分为盘前启动线索与盘中/盘后验证；盘中/盘后材料只有明确记载近期更早事件日期和动作时才能审计该盘前事件，禁止用盘面结果倒推启动原因；LLM 继续使用扁平七字段输出。
- [x] **今日主线板块—个股双向覆盖**：入选个股必须关联入选板块，入选板块也必须至少有一只代表股；日报与面板展示每只强势股对应的入选板块。
- [x] **今日主线跨层级链条补全**：LLM 核心板块仅落在概念或行业单一层级时，脚本按核心股共同覆盖确定性补入最多两个合格的对应行业或概念板块，减少同一输入下板块数量波动。
- [x] **Flash 结构化输出稳定性**：今日主线使用思考模式和 12,000 token 预算，仅在长度截断或空正文时自动切换非思考重试；市场短评保持非思考，失败保留可诊断的 token 信息。
- [x] **全 A 正式初始化**：已回填 300 日线，2026-07-15 覆盖率 4965/4995（99.40%）。
- [ ] **连续盘后积累**：连续运行并验证覆盖率、状态切换和板块排名。
- [ ] **严格点时板块回测**：在积累足够每日板块快照后开展；历史不足部分不得伪装为点时数据。
- [ ] **下游消费决策**：待模块稳定后，由 Bloom / ETF / 交易执行层显式读取 `market/` 输出；不回写 Quant。

### 下一阶段 — 市场解读、策略说明与模块整合

**共享行情数据层：**

- [ ] **共享行情数据层通用化**：将当前市场数据层演进为 Market、Pool、Quant、RPS 等模块共同依赖的本地行情基础设施，统一通达信下载、交易日分界、证券母体、OHLCV 持久化、数据缺口补取与覆盖率校验。
- [ ] **模型二共享数据适配**：不改动 VCP 算法及现有 JSON/CSV/Markdown 输出；为模型二增加可插拔行情提供器，优先读取共享 SQLite，数据窗口不足时通过统一数据层补取并持久化，保留原取数方式作为过渡回退。
- [ ] **共享数据层迁移验证**：对同一交易日、同一股票集合比对共享数据与原模型二数据的 OHLCV、复权/日期口径和 VCP 结果；稳定后再将共享数据设为默认来源。

**市场状态与板块展示：**

- [ ] **市场趋势解读深化**：细化宽基趋势、均线结构、量价、波动与广度的展示证据；保留规则引擎的状态判定，新增 LLM 仅基于结构化指标生成可读解读、风险提示和参与方式建议，不得改写状态或评分。
- [ ] **板块详情卡片**：在行业、概念板块排名下展开阶段状态、20/5 日相对强度与排名变化、绝对收益、广度、量能、强势股/RPS 分布、持续性证据和风险提示，优先服务实际观察与决策。
- [ ] **策略说明页**：在数据分析面板中建立独立的策略定义页，明确市场环境、板块、基本面、RPS、VCP、Bloom、持仓和估值的职责、输入输出、使用顺序与边界；与每日数据报告分离。

**VCP 与市场环境整合：**

### VCP 结构页开发计划

**定位与数据边界：**

- [x] **只读数据适配**：消费 Bloom、模型二、申万二级与市场板块状态，生成 `dashboard/data/vcp_context_<YYMMDD>.js`；不改变 Quant 或 Bloom 的既有 JSON、CSV、Markdown 与状态机。
- [ ] **日期与发布协议**：将 VCP 数据包登记到 dashboard 索引，与市场环境使用同一报告日期；数据缺失时页面明确提示“尚未生成 Bloom 输出”，不得回退展示错误日期。

**页面首版：**

- [ ] **结构总览与筛选**：展示 Bloom 状态数量及 `TRIGGERED`、`MATURE`、`FORMING`、`RISK_BLOCKED` 等分布，支持按状态、买点类型、质量等级和风险等级筛选及排序。
- [x] **VCP 标的列表**：以代码、名称、结构阶段、Bloom 状态、当前/潜在买点、结构质量和风险为核心列；列表只承担筛选和定位。
- [x] **标的详情**：展示逐段收缩时间、幅度与段均量、申万二级及板块状态、结构评分构成与证据、Pivot/支撑/失效价、风险标记、Bloom 生命周期与 LLM 个股解读；明确区分模型二事实与 Bloom 生命周期判断。
- [x] **信号详情市场提示**：列表不增加字段；详情顶部展示同日市场状态标签，并在量价与评分之前按市场状态给出固定规则建议，仅作执行上下文提醒，不改写任何信号或生命周期状态。

**后续增强与验证：**

- [x] **板块环境关联（二期）**：详情页关联所属申万二级及其市场状态，仅作上下文展示，不改写 VCP 结构或 Bloom 状态。
- [ ] **发布校验**：校验面板汇总与 Bloom 原始汇总一致、日期一致、字段缺失可降级展示；用正常日、无候选日、数据缺失日各验证一次。

**模型一：基本面 + RPS 演进：**

- [x] **放开行业限制**：取消模型一行业黑名单，阶段二通过的标的无差别入池；行业字段仅保留用于质量门槛、标签和分布展示。
- [ ] **RPS 策略定义与回看验证**：明确全市场和行业内 RPS 的周期（至少 20/60 日）、横截面口径、更新时点和候选使用方式；仅使用盘后已完成数据，避免前视偏差。
- [ ] **RPS 候选扩展层**：先将 RPS 用于排序、观察池扩展或独立技术强势候选，持续追踪被原基本面筛选漏掉的股票；不得未经验证直接替代基本面硬过滤。
- [ ] **模型一筛选规则升级**：在 RPS 定义与验证完成后，先更新模型一指令卡和策略配置，再实现“基本面 + RPS”筛选与结果输出兼容方案。

---

## 模型二增强：买点预判模块

在模型二 VCP 结构识别基础上，新增**次日买点预判**能力。不改变模型二现有的结构/买点判断逻辑，而是向前推一步：对于已经成熟的 VCP 结构，提前算清楚"明天量价走到哪里会触发哪种买点"。

### P0 — RETEST 放量阴线硬阻断

- [x] RETEST_BUY 不得仅以突破后 5 日平均量判断缩量；当日出现显著下跌且成交量相对前 5 日均量放大、接近突破日量能时，标记 `FAILED_RETEST_SELLING` 并硬阻断买点。
- [x] 将“当前收缩段量能 failed”与 RETEST 买点评分/阻断规则联动，避免供给持续扩张的结构获得 A 级买点。

### P0 — VCP 收缩时效与分组边界

- [x] 相邻收缩间隔超过 25 个交易日切分新 cluster，当前 VCP 仅从最新 cluster 选组。
- [x] 当前 VCP 组首尾跨度限制为 60 个交易日；旧收缩保留审计但不参与当前阶段与买点评分。
- [x] 突破前旧组破位守卫：连续收盘深度跌破最后收缩低点时，旧组失效并等待重建（688333 铂力特复盘）。
- [x] 旧组失效后重建候选必须通过原趋势基础，避免否决旧组后将原 `REJECT` 意外放宽为 `WATCH`（历史回放验证）。

### P1 — 突破后状态管理

- [x] 建立 `PRE_BREAKOUT` / `POST_BREAKOUT_HOT` / `POST_BREAKOUT_RETEST` / `POST_BREAKOUT_CONSOLIDATING` / `POST_BREAKOUT_FAILED` / `POST_BREAKOUT_EXPIRED` 状态机；旧 VCP 突破后永久关闭 PULLBACK 和重复 BREAKOUT。
- [x] 突破后收盘跌破 Pivot × 0.97、回撤过深或时间超期时，关闭旧 VCP 生命周期并等待新 cluster 重建。
- [x] 突破后硬失效采用全路径不可逆判定：任一日跌破失效位或出现深回撤后，旧组及其子组不得因反弹回到 RETEST/VCP_FORMING（688002 睿创微纳回放）。
- [x] 以 002281 光迅科技（旧 VCP 突破后回落）与 002979 雷赛智能（两板突破后受控回踩）作为回归案例，验证“历史结构质量”与“当前交易状态”分离。

### P2 — 收缩量能口径与弱结构买点边界

- [ ] **收缩段量能优先级校正（688531 日联科技案例）**：逐轮 `avg_volume` 明显递增时，不得仅因 `vol_ma20 < vol_ma60` 或局部均量走低而标为 `drying` 并获得结构量能加分。需分别输出“段均量趋势”和“当前均量状态”；若两者冲突，以段均量供给扩张降级结构质量、成熟度或买点权限。
- [ ] **弱 VCP_FORMING 禁止借均线反弹触发 VCP 买点（688183 生益电子案例）**：当收缩幅度未递减且收缩段均量扩张时，即使当前靠近 MA60 且局部缩量，也不得输出 `PULLBACK_BUY` 作为 VCP 买点。可降为独立的均线反弹观察标签，或仅保留观察状态。
- [ ] **Signal Plan 等级口径对齐**：当日已触发的 `FOLLOW` 计划须同时展示模型二实际 `setup_quality`；不得仅以结构分推导的潜在等级让 C 级实际买点在计划中看似升级为 B 级。明确“当日实际质量”与“次日潜在最高质量”的字段和展示方式。

### 核心逻辑

```
输入：模型二 quant JSON（结构阶段、pivot、MA20/MA60、当前收盘、量能状态、收缩段数据）
输出：每只 VCP_MATURE / VCP_TIGHT 标的的次日触发条件清单

for each stock in quant_output where structure_stage in {VCP_MATURE, VCP_TIGHT}:
    计算次日触发三种买点各自需要的量价区间：
      PULLBACK_BUY 触发区间：回踩 MA20/MA60/收敛下沿的位置 + 缩量要求
      BREAKOUT_BUY 触发区间：站上 pivot × 1.01 的价格 + 量能放大要求
      RETEST_BUY 触发区间：先突破再回踩（两段式，次日可能出现的第一段）
    对每种潜在买点预计算预期评分区间
    标注最近的失效位（止损参考）
```

### 买点预判计算规则

**PULLBACK_BUY 预判：**
- [ ] 预判价格区间：`[max(MA20, 收敛下沿) × 0.98, MA20 × 1.02]`
- [ ] 预判量能条件：缩量至 `vol_ma20 × 0.80` 以下，或收缩段低量区
- [ ] 预判评分：基于当前结构阶段 + 回踩位置偏移量 + 缩量程度，给出 A/B/C 级区间
- [ ] 失效位：最近一轮收缩低点

**BREAKOUT_BUY 预判：**
- [ ] 预判价格区间：`[pivot × 1.01, pivot × 1.08]`，细分最佳突破区 `[pivot × 1.02, pivot × 1.05]`
- [ ] 预判量能条件：`volume > vol_ma20 × 1.0`，最佳 `> 1.5×`
- [ ] 预判 K 线条件：收盘接近日高、实体强、无明显上影
- [ ] 预判评分：基于突破幅度 + 量能倍数 + K 线质量，给出 A/B/C 级区间
- [ ] 失效位：pivot 下方或最近收缩低点

**RETEST_BUY 预判（两段式）：**
- [ ] 先判断当前是否已经突破（close > pivot × 1.01）
- [ ] 若已突破：预判回踩位 `[pivot × 0.99, pivot × 1.005]`，回踩缩量条件
- [ ] 若未突破：标注"需先出现 BREAKOUT，再等待回踩"，不生成 RETEST 预判
- [ ] 预判评分：基于回踩位置精度 + 缩量程度 + 重新确认强度
- [ ] 失效位：突破日低点或 pivot 下方

### 产出

- [ ] **instructions/02-quant-prep.md**：买点预判指令卡
- [ ] **scripts/quant_prep.py**：买点预判脚本，消费 `quant/quant_<YYMMDD>.json`，产出预判清单
- [ ] **strategies/02-quant-prep.json**：预判阈值与评分参数配置
- [ ] **输出文件**：`quant/prep_<YYMMDD>.json` 和 `quant/prep_<YYMMDD>.md`（人类可读的次日交易计划）

### 与现有模块的关系

- 买点预判**不替代**模型二和 Bloom 的每日买点判断，而是**辅助次日盘前准备**
- 次日盘后：模型二正常跑出当日实际买点 → 与前一日的预判对比 → 命中/未命中/超预期，记录到回测模块
- Bloom 的 `next_watch_point` 字段当前是自然语言，预判模块上线后可改为结构化触发条件引用

---

## 策略回测模块

以模型二每日 `--date` 回放能力为基础，构建**历史信号回测系统**。核心目标：量化买点评分体系的实际预测能力，用数据驱动策略参数迭代。

### P0 — 滚动价格表现 V1

- [x] 只记录同一轮 VCP 首次 `MATURE_ENTRY` 与首次 `SETUP_TRIGGER`，连续状态不重复计数。
- [x] 每日报告回看信号后 5~20 个交易日窗口，按首次信号日收盘价补齐 5/10/20 日收益，未到期周期留空。
- [x] 冻结首次事件日 Pivot，记录其后首次有效突破时间及突破收盘价高于 Pivot 的幅度。
- [x] 冻结信号日已确认市场状态与申万二级板块状态，分别汇总样本数、胜率和平均收益。
- [x] 新增独立“回测表现”页面，并接入 `daily.py` 自动生成、发布和完整性核验。
- [x] 回测页三个统计模块统一采用全宽纵向排列。
- [x] 统一回测明细和三个统计表的表头、内容列对齐规则。
- [ ] 连续运行积累样本，以第一版基线结果决定下一条需要验证的策略假设。

### 回测维度

**信号胜率统计（按买点类型）：**
- [ ] PULLBACK_BUY 出现后 N 日（5/10/20/40 日）涨跌幅分布
- [ ] BREAKOUT_BUY 出现后 N 日涨跌幅分布
- [ ] RETEST_BUY 出现后 N 日涨跌幅分布
- [ ] 各买点 A/B/C 质量分级后的胜率分化（验证评分体系是否有效）
- [ ] 对比基准：同期中证 500 / 沪深 300 涨跌幅

**结构阶段预测能力：**
- [ ] VCP_MATURE 出现后，N 日内实际触发买点的比例
- [ ] VCP_TIGHT 出现后，N 日内实际触发买点的比例
- [ ] 结构分与后续买点触发概率的相关性

**风险标识有效性：**
- [ ] 有风险标识的买点 vs 无风险标识的买点，后续胜率差异
- [ ] 各类风险标识（EXTENDED_FROM_MA20 / LONG_UPPER_SHADOW 等）的独立预测能力

**Bloom 信号生命周期：**
- [ ] TRIGGERED 信号 N 日后状态分布（仍有效 / COOLDOWN / EXIT）
- [ ] MATURE → TRIGGERED 的平均等待天数
- [ ] COOLDOWN 后重新激活 vs 最终退出的比例

### 技术路线

- [ ] **回放范围**：从 `quant/` 和 `bloom/state/` 最早可用日期起，逐日回放至最近交易日
- [ ] **数据来源**：优先复用缓存中的历史日线数据；缺失部分用 `--date` 回放补全
- [ ] **统计输出**：`backtest/signal_performance_<YYYYMMDD>.csv`（每次运行覆盖更新）
- [ ] **可视化**：买点胜率曲线（按类型/等级的 N 日收益分布箱线图）、信号生命周期桑基图

### 产出

- [ ] **instructions/backtest.md**：回测指令卡
- [ ] **scripts/backtest.py**：回测主脚本，调度历史回放 + 统计计算
- [ ] **strategies/backtest.json**：回测参数（回看天数、N 日窗口、基准指数）
- [ ] **backtest/ 目录**：回测结果（CSV + 图表 + 简报 Markdown）

### 与策略验证的关系

模型四"策略验证"侧重**前向跟踪**（从今天出发，持续观察未来表现），回测模块侧重**后向回测**（回放历史，量化已有信号的实际表现）。两者互补：

| 维度 | 策略验证（前向） | 回测模块（后向） |
|------|-----------------|-----------------|
| 数据来源 | 当日及未来每日运行 | 历史 quant/ + bloom/state/ |
| 时效 | 随交易日累积 | 一次性回放全部历史 |
| 用途 | 跟踪当前策略表现、早期预警 | 验证评分体系、校准参数阈值 |
| 更新频率 | 每日追加 | 按需运行（参数调整后重跑）

---

## 模型三：Valuation 重构

### P0 — LLM 脚本化

- [x] **估值报告研究框架重构**：以“利润支柱 → 一致预期 → 市场分歧 → 叙事期权/预期差 → 三情景估值 → 验证节点”为正文与研究卡契约；旧的事实/参数拼接不再可作为完成报告。
- [x] **模型三 v2 可审计编排基座**：新增 `valuation_pipeline.py`，支持主动单股触发、运行包归档、证据 ID 门禁、受 JSON schema 约束的 LLM 研究卡、参数校验、引擎调用和模板化报告渲染；搜索失败/证据不足/LLM 输出异常均显式失败，不得补写事实。
- [x] **模型三固定检索清单**：编排器自动采集财报/业务、研报预测、公告订单、管理层动作、可比估值和市场价格；原始响应按标的/主题缓存，支持有效期、强制刷新、退避重试和显式失败记录。
- [x] **模型三专题证据去重与阶段复用**：阶段三仅读取本次专题计划产出的缓存并按结果级标识去重；阶段四复用阶段三阅读结论，只补充增量验证材料，不重复阅读原始专题证据。
- [x] **模型三 Dashboard 适配层**：仅从完成的 v2 运行包发布 `valuation_context` 数据包；主面板提供已完成标的的估值索引与状态，点击进入独立公司研究页展示 Layer 1/2/3、假设、风险催化剂、共识状态和运行证据，不解析 Markdown 或读取未验证缓存。
- [x] **LLM 研究卡长响应稳定性**：分阶段请求具备超时/重试、HTTP 状态、请求响应长度、原始响应 trace 和 JSON 错误定位；原文阅读逐批缓存，结构修复与研究审计使用小型独立节点，失败不再重读前序材料。
- [x] **模型三阶段一~五脚本化**：共识研报提取、可比/机构估值锚、专题检索、预期差、验证节点、通用参数映射、研究完整性审计均由 `valuation_pipeline.py` 驱动，用户只需审阅最终运行包。
- [x] **模型三通用映射与进度页**：映射仅依赖标准枚举、`pillar_id`、路线和显式引擎参数；支持 Layer 2 累加、跨路线 Type B、断点复用；Dashboard 展示运行中标的及大阶段/子步骤实时进度。
- [x] **模型三最新投研目录与完整报告页**：按标的聚合最新已验证运行，修复代码误作名称及历史进度重复；基于运行包数据完整展示一致预期、PE 构建、三层矩阵、利润支柱、分歧、叙事期权和验证节点，页面重建不触发搜索或 LLM。
- [x] **模型三当前价口径修复**：当前价改由统一日线数据服务按最新有效交易日获取，记录价格日期和来源；禁止使用财务报告期市值反推当前价，支持复用研究卡只重算估值空间与页面数据。
- [x] **模型三端到端总控**：新增 `run_valuation.py`，统一驱动 mx-data 财务缓存自动补齐、阶段零、五阶段编排、运行锁、断点恢复、终态校验和执行摘要，日常运行不再依赖对话逐条串联命令。
- [x] **模型三阶段五稳定化**：研究结论与参数映射分别按三类有界子节点执行并可独立续跑；补齐亿元/字段类型/渲染契约、机构分歧口径、支柱净利合计、PE 语义与 Layer 2 去重门禁，并以金盘科技运行包完成真实回归。
- [x] **模型三投研页决策视图与分包加载**：公司页增加双年度三情景首屏、粘性章节导航和 2026E/2027E 三层矩阵切换；最新目录、公司研究与证据拆包发布，正文证据按 `source_id` 懒加载定位。
- [x] **模型三统一分支柱估值模型**：阶段二新增分支柱双年度三情景利润/倍数/概率/估值表和假设账本契约；主营与 REIT、资产处置等独立事项统一列入一张模型、分别估值，机构锚按利润/估值口径匹配并剔除异常偏离参考值；后续证据只通过利润、倍数或独立事项概率进入模型。Dashboard 支持年度及支柱切换，删除重复三情景估值块；报告、排名和页面统一使用该模型，旧引擎结果仅保留为迁移审计。
- [x] **模型三经营利润总量守恒**：阶段二从 `recurring|unknown` 且无明确非经常污染的机构预测确定双年下沿/中位/上沿控制利润；业务地图可细拆，但缺少直接分部利润与匹配估值锚时必须合并经营估值支柱。脚本逐年度逐情景对账，单支柱以公司共识覆盖，可靠分部仅允许5%以内对齐，禁止拆分造成利润凭空增减；Dashboard展示利润来源与对账控制数。
- [x] **模型三PE锚不足与历史事项去重**：`cross_check_only`全部进入排除名单；同口径直接/可转换PE与数值公司可比不足2项时，以至少3个交叉检查PE的保守下半区中位数执行显式回退，不得伪装成机构目标估值。历史已实现非经常损益前瞻年度自动归零，只有可证明未被其他支柱计价的留存净现金/净资产才能单列价值。
- [x] **模型三V4共识估值简化**：阶段二改为公司整体机构一致性预期估值，业务地图不再强制分配利润和估值；当前股价隐含PE不得进入估值，只保留明确目标PE或目标价隐含PE的同研报组合。Dashboard按“共识估值—业务地图—逐机构利润逻辑—机构明细—分歧—验证—风险”展示，删除PE二次构建、可比、重复事实假设与催化剂模块。
- [x] **模型三估值支柱准入与叙事分流**：阶段一业务候选地图逐项映射为独立估值、合并计价、叙事观察或历史排除；凡称估值支柱必须进入统一估值表。阶段三按最小经济事项拆分，概率型事项必须具备双年三情景利润/价值、倍数、概率、增量口径和证据，阶段五不得再降级排除；无法量化项目仅保留叙事与验证节点。
- [x] **模型三阶段三证据时限门禁**：仅使用实际进入阶段二估值组合且经原始证据日期校验的机构研报形成上中位日与最晚日双边界；动态材料分为背景、重叠窗口、共识后新增和日期未核验四类，只有晚于最晚研报日的共识后新增材料进入阶段三阅读。概率事项必须引用新增证据并证明阶段二未纳入；无合格新增材料时跳过阶段三研究 LLM，门禁指纹变化自动使阶段三至五失效重做。
- [x] **模型三业务地图覆盖展示关联**：Dashboard 保留阶段一完整业务地图，并按稳定 `pillar_id` 合并阶段二逐项机构覆盖结论、原因与证据；仅对缺少覆盖数据的历史运行包显示待判断文案。
- [x] **模型三逐机构利润逻辑与跟踪假设**：阶段二逐机构保存有证据的经营驱动→收入→利润链条、明确缺失环节，并把机构成立假设映射为后续跟踪指标和失效信号；契约校验禁止倒推经营参数或改写机构利润，Dashboard 按链条与跟踪双栏展示。
- [x] **模型三报告页证据索引精简**：公司报告页移除不可稳定访问的证据 ID、索引按钮、披露标签及原始证据目录，不再发布独立证据数据包；运行包继续保留 `source_ids` 和证据目录供内部契约校验。
- [x] **模型三报告页内部枚举隐藏**：一致预期明细和逐机构利润卡片不再展示 `profit_scope` 及其技术枚举；字段仅保留在运行包中用于非经常性口径清洗。
- [x] **模型三估值页重复摘要精简**：一致性估值表移除对应机构列及双年度估值方块，估值结论移除主要支柱/首要风险/最近验证节点三卡摘要；底层情景来源和决策摘要继续保留在运行包中。
- [x] **模型三结构化公司核心摘要**：阶段五A1新增有证据的公司画像、量化盈利共识、增长逻辑、关键不确定性和跟踪重点五段摘要，禁止用泛化判断代替；Dashboard 在估值结论前以统一总结栏展示，旧单句核心判断退出页面。
- [x] **模型三一致性估值表聚焦**：估值表仅展示情景、净利润、目标PE、估值和取值说明，移除重复的目标价列；目标价原始数据仍保留在机构预测明细和运行包中。
- [x] **模型三估值与一致预期合并展示**：估值结论和一致性预期估值表合并为一个板块，共用单一双年度切换；价格区间、轨道和三情景估值明细在同一视图联动更新。
- [x] **模型三表格数值展示对齐**：机构明细中的零值统一显示破折号，估值表和机构明细表按文本列左对齐、数值列表头与数据右对齐，并使用等宽数字减少视觉错位。
- [x] **模型三业务支柱价值画像V1**：阶段五按阶段一完整业务地图逐项输出公司作用、盈利模式、已披露核心指标、增长潜力、估值锚定、跟踪指标与数据缺口；画像仅用于解释和跟踪，不拆分公司整体利润或改写一致性估值。
- [ ] **consensus 字段升级**：接入真实分析师一致预期数据源，减少 `auto_generated` 占比
- [ ] **脚本共识质量验证**：补充交叉验证逻辑

### P1 — 参数优化

- [ ] **决策树阈值校准**：Q2（固产/总资产 40%/20%）、Q3（毛利率波动 5pct）分界线基于实际案例回测调整
- [ ] **路线 E/F 标的 Q2 跟踪**：Q2 季报发布后重跑决策树

---

## 自选股同步模块重构

### P0

- [x] **scripts/sync_zixuan.py 重构**：根据模型四产出维护自选股
  - 每日删除本地账本记录的上一日工作流自选，再写入 Bloom 日报重点观察和三类买点标的
  - 重点观察口径：成熟结构，或 FORMING/EARLY 且结构分至少 60；买点不受分数限制
  - 接入 `daily.py` 最后一步，刷新后做目标集合校验
  - 操作仅对东方财富自选股"全部"分组生效，保留同分组的手工自选
- [x] **instructions/sync-zixuan.md**：自选股同步指令卡
- [ ] **估值门槛机器字段接入**：模型三新增明确的估值通过枚举后，接入自选股目标集合；不得从 Markdown 文本猜测

---

## 已完成

- [x] 模型二当前 VCP 组选择优化（明显扩张重置旧收敛结构）
- [x] 模型二 VCP 买点优化（PULLBACK 段间缩量确认 + BREAKOUT_BUY）
- [x] Bloom 信号层重构（bloom.py + signal-bloom.md + 04-bloom.json）
- [x] Bloom 日报分层重构（4 板块清晰结构）
- [x] RETEST_BUY 逻辑修复（移除 old_structure_broken_out）
- [x] 数据层统一抽象（DataSource + fetch_daily + TDX 降级）
- [x] 策略配置化（strategies/ + strategy_config.py）
- [x] DESIGN.md + WORKFLOW.md 设计文档
- [x] v1.2 发布
- [x] 数据口径统一（周期感知阈值）
- [x] shared.py 共享模块
- [x] 指令卡 Git 管理（固定文件名）
- [x] v1.3 发布
- [x] v1.4 / v1.4.1 发布（Bloom 信号层稳定）
- [x] 模型二买点评分体系（PULLBACK + RETEST 双买点 + 质量分）
- [x] 模型二四段式买点评分重构（stage_base + action_type_base + action_quality_score + risk_adjust）
- [x] BREAKOUT_BUY 完整体系（硬条件 + 动作质量评分 + K线确认）
- [x] 模型二当前 VCP 组收缩扩张重置逻辑优化
- [x] Bloom 信号层 v2（BREAKOUT_BUY 补全 + 状态字段扩展 + LLM 逐只请求）
- [x] Bloom 突破后状态映射修复（`structure_stage=NONE` 时优先消费 `post_breakout_state`，避免误兜底为 `FORMING`）
- [x] 重点观察智能过滤（结构分门槛）与排序规则（买点优先 + 结构强弱）
- [x] 交易策略参考文档（instructions/trading-strategy.md）
- [x] 日线缓存 30 天保留 + --date 回测参数
- [x] 模型四 Signal Plan v1（次日量价触发计划：signal-plan.md + signal_plan.py + 04-signal-plan.json）
- [x] 模型四 Tracker 总控 v1（编排 Bloom + Signal Plan → 合并日报；清理旧 tracker.py/generate_report.py/signals/）
- [x] v1.5 发布
